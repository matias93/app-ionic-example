import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'operation-geozone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
